# 0.0.37 Cohesion Cleanup

## Scope
- Remove legacy visibility contexts and command registrations left from the former Activity Bar Compare Results and Show Log views.
- Update active AAMAD and README planning references after moving release planning documents into `project-context/docs/`.

## Acceptance Criteria
- Compare Results and Show Log remain opened and closed through their editor webview panel lifecycle.
- `gitRefs.hideCompareResults` and `gitRefs.hideShowLog` are not registered as extension commands.
- Planning references point to the current `project-context/docs/` location.

## Verification
- `npm run build`
- `npm test`
