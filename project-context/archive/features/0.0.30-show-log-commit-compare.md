# Show Log Commit Compare

## Build Status

Build implementation complete for the `0.0.30` Show Log commit compare workflow.

Version bump, package generation, and Marketplace publishing remain unapproved.

## Goal

Let users compare two commits directly from Show Log by selecting the first commit, Ctrl-selecting the second commit, and choosing `Compare` from the commit context menu.

## Implementation Summary

- Replaced the single local Show Log selected commit with an ordered, two-commit local selection.
- Preserved normal click behavior: selects one commit and expands or collapses details.
- Added Ctrl-click behavior: updates comparison selection without expanding or collapsing details.
- Added a `Compare` commit context-menu action that appears only when two selected commits include the context-menu commit.
- Added `Compare with Worktree` for the single selected commit context-menu state.
- Added bounded `compareCommits` webview message validation.
- Added bounded `compareCommitWithWorktree` webview message validation.
- Injected `CompareResultsPresenter` into `ShowLogViewProvider`.
- Added testable commit-compare helpers that call `repository.diffBetween` or `repository.diffWith` and open the existing Compare Results view.
- Added workbench view layout intents so Show Log and Compare Results hide the Graph view while active and take the available vertical space, with resize commands as fallback.
- Compare results opened from Show Log now minimize Show Log while active and restore Show Log height when the compare view is closed.
- Added startup activation so the Graph visibility context is initialized before the context-controlled Graph view is needed.

## Touched Surfaces

- `src/compareResultsView.ts`
- `src/extension.ts`
- `package.json`
- `src/refActions/types.ts`
- `src/showLog/messageValidation.ts`
- `src/showLog/commitCompare.ts`
- `src/showLogView.ts`
- `src/showLogWebview.ts`
- `src/viewLayout.ts`
- `test/showLogCommitCompare.test.ts`
- `test/showLogWebview.test.ts`
- `test/viewLayout.test.ts`
- `test/webviewMessageValidation.test.ts`

## Acceptance Criteria

- Normal click still selects and toggles commit details.
- Ctrl-click adds a second commit to the comparison selection without toggling details.
- A third Ctrl-click keeps only the previous latest selection and the new commit.
- `Compare` is shown only when two commits are selected and the menu opens over either selected commit.
- `Compare with Worktree` is shown when exactly one commit is selected and the menu opens over that commit.
- Compare Results opens with the first selected commit as base and the second selected commit as compare.
- Compare Results opens against the worktree for single-commit worktree comparisons.
- Show Log opens expanded to the available vertical space.
- Compare Results opens expanded to the available vertical space for graph-originated worktree comparisons.
- Compare Results opened from Show Log minimizes Show Log until the compare view is closed, then Show Log expands again.
- Graph is hidden while Show Log or Compare Results is active and restored after the last secondary view closes.
- The extension activates on startup to avoid a hidden Graph view blocking its own activation.
- Missing or unloaded commit hashes are ignored.
- Empty diffs show an informational message instead of opening Compare Results.

## Test Plan

- Webview shell coverage for ordered selection, Ctrl-click, context-menu `Compare`, `Compare with Worktree`, and compare post messages.
- Message validation coverage for valid and oversized `compareCommits` and `compareCommitWithWorktree` payloads.
- Helper coverage for successful compare, worktree compare, unloaded commit guard, and empty diff messages.
- Manifest coverage for startup activation and context-controlled Graph visibility.
- View layout helper coverage for hiding/restoring Graph, maximizing active secondary views, minimizing Show Log before log-originated compare, and fallback resize commands.
- `npm run build` passed.
- `npm test` passed with 269 tests.

## Manual Validation

- Open Show Log.
- Click one commit and confirm details still expand or collapse.
- Ctrl-click another commit and confirm details are not toggled.
- Right-click the second selected commit and confirm `Compare` appears.
- Click `Compare` and confirm Compare Results opens with file-level differences.
- Ctrl-click one commit, right-click it, and confirm `Compare with Worktree` opens Compare Results against the current worktree.
- Close Compare Results after a log-originated compare and confirm Show Log returns to the available vertical space.

## Release-Note Impact

Mention as Show Log workflow improvement: commits in Show Log can now be compared through Ctrl-selection and the context menu.
