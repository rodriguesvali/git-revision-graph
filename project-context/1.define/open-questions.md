# Open Questions

- Should `0.0.19` include only tag creation, or also one small polish item if implementation risk stays low?
- Should the first tag creation slice support annotated tags, or only lightweight local tags?
- What manual Extension Development Host validation matrix should be required before Marketplace publishing?
- Should future feature artifacts be named by version, by feature slug, or both?
