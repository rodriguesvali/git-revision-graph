# 1.0.0 Release Readiness Closure

## Goal

Close the `1.0.0` polish cycle with release evidence, release notes, and completed handoff notes after maintainer smoke validation, VSIX packaging, and Marketplace publication.

## Context

`1.0.0` was opened after the published `0.0.39` hardening release as a focused polish release. The package metadata is bumped to `1.0.0`, the implemented work is limited to small graph, push, checkout, and Show Log workflow refinements, and the VSIX/Marketplace release has been completed by the maintainer.

## Scope

- Confirm `package.json` and `package-lock.json` remain on `1.0.0`.
- Keep `CHANGELOG.md` aligned with the implemented polish.
- Update `project-context/3.deliver/release.md` from open-cycle language to completed-release handoff language.
- Update deployment and operations artifacts so they reference `1.0.0`.
- Record that VSIX packaging and Marketplace publication were completed after maintainer approval.

## Non-Goals

- No version bump beyond `1.0.0`.
- No additional VSIX packaging beyond the completed `1.0.0` package.
- No additional Marketplace publication beyond the completed `1.0.0` release.
- No new feature scope beyond the completed polish items.

## Acceptance Criteria

- Release readiness notes mark `1.0.0` as released.
- Completed automated verification is recorded with the latest `npm test` count.
- VSIX packaging and Marketplace publication are recorded as complete.
- Deployment and operations notes identify `1.0.0` as the latest published release.

## Verification

- `package.json` reports version `1.0.0`.
- `npm test` passed with 410 tests after the final Show Log cherry-pick fallback polish. This includes `npm run build` through the test script.
- `git diff --check` passed after release-artifact updates.
- VSIX package `git-revision-graph-1.0.0.vsix` exists in the repository root.
- Marketplace publication for `1.0.0` is complete.

## Handoff

Completed maintainer handoff:

1. Extension Development Host/release validation completed by the maintainer.
2. VSIX packaging completed after approval.
3. Marketplace publication completed after approval.
4. Post-release monitoring should follow `project-context/3.deliver/release.md` and `project-context/3.deliver/operations.md`.
