# 2.0.0 Flow Governance Icon Badges

Status: Implemented; automated validation complete; tooltip-refinement manual follow-up pending
Updated: 2026-07-17

## Goal

Reduce the horizontal footprint of Flow Governance branch-kind badges and align their visual
language with VS Code without removing textual or assistive identification.

## UX Decision

- Replace `MAIN`, `REL`, `SYNC`, `PKG`, `FEAT`, `TASK`, `BUG`, `HOTFIX`, and `?` in graph cards
  with selected Codicon glyphs.
- Let the reference row retain the primary ref-state color signal; render graph badges as quiet
  tonal containers that inherit that row's foreground instead of adding saturated kind colors.
- Reuse the same tonal icon badge in Flow Governance reference tooltips. Keep known Flow kinds
  icon-only because their visible labels repeat information already conveyed by the branch name;
  pair only the unknown icon with a quiet visible `Unknown branch` label. Replace the neutral
  `HEAD`, `BRANCH`, `REMOTE`, `TAG`, and `STASH` tooltip chips with the established Show Log icons,
  while retaining a textual fallback for unexpected reference kinds.
- Give each icon-only badge an accessible `<kind> branch type` label.
- Let the visible `Unknown branch` text provide that tooltip's accessible kind name without
  duplicating speech from its question-mark icon.
- Keep unknown or unsupported kinds fail-soft with the question glyph and their supplied label.
- Use a high-contrast-only outline and no hover chrome because the badge is metadata, not a button.

## Mapping

| Flow kind | Codicon |
| --- | --- |
| Main | `home` |
| Release | `rocket` |
| Sync | `sync` |
| Package | `package` |
| Feature | `lightbulb` |
| Task | `tasklist` |
| Bug | `bug` |
| Hotfix | `flame` |
| Unknown | `question` |

## Neutral Reference Mapping

| Reference kind | Show Log shape |
| --- | --- |
| HEAD | Target |
| Branch | Git branch |
| Remote | Cloud |
| Tag | Tag |
| Stash | Archive box |

## Scope And Constraints

- Keep rendering browser-only and CSP-safe through trusted inline SVG.
- Add no runtime dependency, resource root, font source, setting, command, protocol field, or Git
  behavior.
- Attribute the adapted Codicon glyph paths in `THIRD_PARTY_NOTICES.md`.
- Preserve Flow Governance-disabled rendering and every existing reference interaction.

## Acceptance Criteria

- Every classified graph reference renders the mapped icon without its old abbreviation.
- Graph badges are 18px tonal containers with 12px icons and no button-like border.
- Icons inherit the current reference-row foreground; Task receives an 11px optical adjustment.
- High-contrast themes receive a `--vscode-contrastBorder` outline without changing layout.
- Flow Governance tooltips reuse the same tonal icon badge. Known kinds remain icon-only with an
  accessible label, while only unknown kinds add a visible `Unknown branch` label.
- Neutral HEAD, branch, remote, tag, and stash tooltips use the matching Show Log shape in the same
  18px tonal container, expose a `<kind> reference` accessible label, and add no visible text.
- Unexpected neutral reference kinds retain the escaped textual chip fallback.
- The badge exposes an accessible branch-type label and the nested SVG is decorative.
- The reference tooltip visibly exposes `Unknown branch` without adding redundant visible labels
  to Main, Release, Feature, and the other known kinds.
- All mappings and fallback behavior are covered by typed webview tests.

## Verification

- Focused typed webview tests passed 76/76.
- `npm run quality:check` passed with 223 production files and 2,110 functions.
- `npm run build`, all 751 tests, and `git diff --check` passed.
- `graphify update .` rebuilt 4,514 nodes, 8,865 edges, and 360 communities after the UX
  refinement.
- Visual Extension Development Host validation completed by maintainer confirmation on
  2026-07-17.
- The final unknown-only tooltip label passed `npm run quality:check` with 223 production files and
  2,110 functions, `npm run build`, all 751 tests, and `git diff --check`. `graphify update .`
  rebuilt 4,515 nodes, 8,866 edges, and 361 communities.
- The neutral reference-icon refinement passed `npm run quality:check` with 224 production files
  and 2,111 functions, `npm run build`, all 753 tests, and `git diff --check`. `graphify update .`
  rebuilt 4,521 nodes, 8,870 edges, and 362 communities.

The first visual review exposed the graph's broad absolute-positioning rule for `svg` elements
also affecting badge glyphs. `.flow-badge-icon` now explicitly restores static positioning and an
automatic inset, keeping each glyph centered by its badge's inline-flex layout.

A second UX review found that saturated per-kind fills and dark borders made the icons resemble
miniature controls and competed with the reference-row colors. The graph badge treatment now
inherits the row foreground over a 12% tonal fill, has no normal-theme border, and uses a
high-contrast-only inset outline. Release uses `rocket` and Feature uses `lightbulb` for stronger
recognition at compact sizes. A follow-up visual review extended the same tonal icon treatment to
Flow Governance tooltips, replacing their remaining colored textual chip while retaining the full
kind for assistive technology; non-governed reference kinds keep their neutral textual chip. A
subsequent learnability review initially added the full kind visibly beside every tooltip icon.
Maintainer review found those labels redundant when the branch name already communicates a known
kind, so the final treatment keeps known tooltips icon-only and reserves visible text for
`Unknown branch`, where the question-mark glyph cannot provide a meaningful classification.
The neutral reference-kind review then replaced `HEAD`, `BRANCH`, `REMOTE`, `TAG`, and `STASH`
chips with the existing Show Log target, branch, cloud, tag, and archive shapes. Reusing the tonal
container keeps those icons visually aligned with Flow Governance without adding kind colors or a
new icon language; the full reference name beside each icon supplies the visible context.

## Manual Validation

Result: Original icon-badge review completed by maintainer confirmation on 2026-07-17; targeted
unknown-label and neutral reference-icon follow-ups remain pending.

- Enable Flow Governance and inspect all available kinds at 100%, 125%, and 150% UI zoom.
- Compare light, dark, and high-contrast themes for icon legibility and color contrast.
- Hover and keyboard-focus each known kind and verify its tooltip remains icon-only with an
  accessible `<kind> branch type` label. Verify only the unknown kind visibly displays
  `Unknown branch` and does not duplicate it for assistive technology.
- With Flow Governance disabled, verify HEAD, branch, remote, tag, and stash tooltips show their
  respective icon-only neutral badge and announce the complete `<kind> reference` label.
- Verify the remote cloud remains understandable beside an `origin/...` name and that an unexpected
  reference kind falls back to an escaped textual chip.
- Verify long branch names gain usable horizontal room and remain aligned with remote refs.
- Confirm `lightbulb`, `rocket`, `home`, and `flame` communicate Feature, Release, Main, and Hotfix
  clearly enough; verify specifically that `rocket` reads as a release branch rather than a
  deployment action.

## Release-Note Impact

Flow Governance branch classifications now use compact, theme-adaptive VS Code-aligned glyphs;
known tooltip kinds remain icon-only, while unknown classifications are explicitly identified as
`Unknown branch`. Neutral Git reference kinds now use the matching icon language from Show Log.

## Touched Files

- `src/revisionGraph/webview/script/referenceTooltip.ts`: renders icon-only known kinds, visible
  text only for unknown Flow kinds, and a textual fallback for unexpected neutral kinds.
- `src/revisionGraph/webview/runtime/referenceKindPresentation.ts`: owns neutral reference icons.
- `src/revisionGraph/webview/styles.ts`: keeps the unknown label quiet and aligned with its icon.
- `tsconfig.webview.json` and `scripts/build-webview-test-runtime.mjs`: bundle and test the typed
  neutral presentation module.
- `test/revisionGraphWebview.test.ts`: covers neutral icons, fallback, Flow precedence, and Unknown.
- This focused artifact and `project-context/3.deliver/release.md`: record UX and gate evidence.
