# 1.5.8 Dead Code Cleanup

Status: Complete
Last updated: 2026-07-03
Target version: `1.5.8`

## Goal

Remove implementation proven unreachable from the extension entrypoint, active editor-panel flow,
webview runtime, and automated tests without changing user-visible behavior.

## Scope And Acceptance Criteria

- Remove unreferenced commit-graph query helpers and legacy server-side node markup.
- Remove the obsolete `WebviewView` controller adapter after the product moved to an editor panel.
- Remove uncalled controller wrappers superseded by focused workflows.
- Preserve the dynamically loaded d3-dag layout worker.
- Do not change commands, menus, views, settings, dependencies, package version, or Marketplace data.
- Pass the TypeScript build, full automated test suite, and whitespace validation.

## Delivery Handoff

- Removed seven unreachable exports or methods plus the orphaned `WebviewView` surface adapter.
- Preserved test-only seams and compatibility exports that were not proven to be dead implementation.
- No user-visible behavior, packaging metadata, or release process changed.

## Recorded Verification

- `npm run build` passed on 2026-07-03.
- `npm test` passed with 525 tests on 2026-07-03.
- `git diff --check` passed on 2026-07-03.
- Static reference scanning found no remaining references to the removed controller methods,
  commit-graph helpers, markup renderer, constant, or `WebviewView` adapter.
- Manual Extension Development Host validation remains pending.
