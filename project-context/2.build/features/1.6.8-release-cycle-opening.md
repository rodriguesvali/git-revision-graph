# 1.6.8 Release Cycle Opening

Status: Cycle opened; functional scope pending
Target version: `1.6.8`
Opened: 2026-08-22
Published baseline: `1.6.7`

## Goal

Open the `1.6.8` development cycle from the published `1.6.7` baseline while keeping package
metadata, release governance, and implementation scope explicit.

## User Workflow

No user-facing workflow changes at cycle opening. Individual workflows will be documented in
focused feature artifacts when their scope is approved.

## Scope

- Align `package.json` and the root `package-lock.json` to `1.6.8`.
- Record `1.6.7` as the published baseline for this cycle.
- Keep implementation, verification, packaging, and publication gates pending until feature scope
  is approved and delivered.

## Non-goals

- No runtime, command, menu, setting, dependency, or documentation behavior change.
- No VSIX packaging, Git tag, Marketplace publication, or installed-version claim.
- No release-date or feature commitment before scope approval.

## Touched Surfaces

- `package.json`
- `package-lock.json`
- `project-context/2.build/features/README.md`
- `project-context/2.build/features/1.6.8-release-cycle-opening.md`
- `project-context/3.deliver/release.md`

## Acceptance Criteria

- Both package manifests declare `1.6.8`.
- Release readiness identifies `1.6.7` as the published baseline and `1.6.8` as the open cycle.
- All implementation, smoke, packaging, and publication gates remain explicitly pending.
- No source or dependency changes are introduced by the cycle opening.

## Risks

- Package metadata could drift between the two manifests.
- An open development version could be mistaken for a release candidate without explicit gate
  status.

## Test Plan

- Verify the two manifest versions directly.
- Run `npm run build` because the package surface changed.
- Run `git diff --check` and review the final diff.

Recorded on 2026-08-22:

- Manifest consistency check passed for `package.json`, `package-lock.json`, and the lockfile root
  package entry.
- `npm run build` passed.
- `git diff --check` passed.
- `graphify update .` refreshed the repository knowledge graph.

## Manual Validation

No Extension Development Host smoke is required for this metadata-only opening. Feature-specific
manual checks will be recorded when user-visible scope is approved.

## Release-note Impact

None at cycle opening. Candidate notes remain pending approved functional scope.

## Handoff Notes

- Create or update focused `1.6.8` feature artifacts before implementation begins.
- Do not package, publish, tag, or alter Marketplace state without explicit maintainer approval.
