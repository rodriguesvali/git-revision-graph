# 1.5.0 Release Opening

## Approval

Status: approved and completed on 2026-06-27.

The maintainer confirmed that all `1.4.0` Deliver gates are complete, including Extension Development Host smoke validation, VSIX packaging, clean-profile installation, and Marketplace publication, and authorized opening the next release as `1.5.0`.

Opening comparison baseline: `0fd5263e5fdbcaaeb9d305abf981f7e01e895936`.

## Goal

Open the `1.5.0` AAMAD cycle as a reliability and safety release after the published `1.4.0` release.

## Scope

- Record maintainer confirmation that all `1.4.0` Deliver gates are complete.
- Bump `package.json` and root package metadata in `package-lock.json` to `1.5.0`.
- Open `1.5.0` release readiness.
- Add an empty `1.5.0` release-preparation changelog section without claiming unimplemented fixes.
- Keep the robustness plan proposed until the remaining technical decisions and scope approval are resolved.

## Non-Goals

- No robustness implementation in the opening step.
- No dependency, command, menu, view, setting, or contribution-point change.
- No `1.5.0` VSIX packaging or Marketplace publication.
- No automatic approval of the proposed implementation slices.

## Acceptance Criteria

- Package metadata reports `1.5.0` in `package.json` and the root package entries of `package-lock.json`.
- `project-context/3.deliver/release.md` records `1.4.0` as published and `1.5.0` as open.
- The resolved `1.4.0` publication question is recorded in `project-context/1.define/open-questions.md`.
- The remaining `1.5.0` technical questions remain explicit.
- `CHANGELOG.md` does not claim unimplemented reliability fixes.
- `git diff --check`, `npm run build`, and `npm test` pass.

## Verification

- Package metadata confirmed at `1.5.0` in `package.json`, root `package-lock.json`, and root package-lock package metadata.
- `git diff --check` passed.
- `npm run build` passed through the test lifecycle.
- `npm test` passed with 466 tests.

## Release-Cycle Follow-Up

- Review the proposed scope in `project-context/docs/release-1.5.0-prioritization.md`.
- Resolve mutation coordination, restore strategy, Git execution profiles, layout fallback, platform fixtures, CI, dependency updates, and benchmark questions.
- Freeze approved slices before Build begins.
- Keep packaging and publication as separate Deliver approvals.
