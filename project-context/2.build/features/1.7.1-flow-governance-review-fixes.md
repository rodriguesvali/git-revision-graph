# Flow Governance baseline review fixes — 1.7.1

Status: implemented; automated verification passed; manual smoke pending
Date: 2026-09-06

## Define

Address both confirmed P2 findings from review against baseline
520f300fea16a3f43480136e4637866bb30c60a0: same-path repository wrapper replacement must not strand the
toggle in Saving, and a transient unavailable preview must be retryable without changing the name.
Preserve disposal, different-repository isolation, duplicate-write prevention and preview correlation.

## Build

- `flow/optionsWorkflow.ts` keys pending writes by repository path and uses that path consistently for
  completion, saving cleanup, error feedback and opening a newly created configuration. A refreshed
  wrapper cannot bypass duplicate-write prevention or suppress the completed persisted state.
- `runtime/flowFormPreview.ts` clears the cached request key for unavailable responses without a
  structured validation summary and binds an explicit Retry preview action. Retrying generates a new
  request identity for the same inputs. Reset removes the callback; callbacks cannot cross repositories.
- `runtime/flowPreviewSummary.ts` adds a normal keyboard-accessible retry button, visible only for
  transient/configuration/authorization failures. Structured name errors continue to require editing.
  Hiding a focused retry moves focus to the status. The existing submission lifecycle disables it.
- Runtime and orchestration regressions cover same-path replacement on save success/failure,
  duplicate write rejection across wrappers, identical-input retries, stale replies, repository
  switching, close/reset and the absence of retry for a structured validation error.

## Deliver

`npm test` passed 922 tests, including build, isolated webview typechecks and quality gates.
`git diff --check` passed and `graphify update .` completed. Manual smoke: toggle while refreshing the same repository, exercise
write failure, trigger an unavailable preview during loading, then Retry preview with unchanged input;
verify keyboard focus and close/reopen. Retry can fail again if the repository/configuration is still
unavailable; no automatic retry loop is introduced. No dependencies, version, packaging or publication
changes. Rollback this slice without touching repository configuration or branches.


## Final release-review corrections — 2026-09-06

Scope: resolve the remaining P2 configuration-opening race and the Package description-width P3.
`governanceWorkflow.ts` now checks the repository path during both config inspection and document
reveal. Tests replace the wrapper at both await boundaries and verify that different-repository and
disposal transitions still suppress the reveal. The description row uses flex layout so a hidden
AI button consumes neither a fixed column nor its gap; visible buttons retain their 26 px space.
No Git behavior, dependencies or version changes. `npm test` passed 928 tests including build and quality gates; diff checks and Graphify update passed. Manual smoke:
Open Configuration during refresh; switch/close during opening; compare Name/Description widths for
Package and confirm visible AI buttons remain aligned for Feature/Release. Previous publication
cancellation and installed-VSIX release checks still require manual confirmation.
