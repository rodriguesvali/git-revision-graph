# Flow Governance early name validation — 1.7.1

Status: implemented; automated verification passed; manual smoke pending
Date: 2026-09-05

## Define

Fifth authorized UX increment: identify branch-name conflicts before submission in branch creation
and equalization. Reuse configured name resolution and collision semantics, including case-only
collisions, and give a valid example when a name is incomplete or invalid.
Acceptance: feedback updates through the existing debounced, repository-scoped preview; known local
and remote-tracking collisions identify the conflicting branch; examples honor custom patterns.
The preview remains advisory and never fetches, reserves names or changes Git state. Submission keeps
its full collision and readiness checks because the snapshot can change or omit remote branches.

## Build

- `flowBranchNameCollision.ts` extracts a shared snapshot lookup from the existing submission check;
  submission retains its additional getBranch lookup. Preview reads HEAD and known refs only.
- `flowFormPreview.ts` marks collisions unavailable and uses the existing actionable collision text.
  When no collision is known, explicitly describe the snapshot limit and submission recheck.
  Incomplete/invalid names show a resolver-validated example, or the required pattern if the sample
  does not match. Equalization preserves its target/base and merge-direction explanation.
- `formWorkflow.ts` supplies repository-scoped snapshot lookup after configuration and authorization.
  Existing preview correlation, safe text rendering, debounce and lifecycle guards remain applicable.
- No protocol, dependency, version, mutation or submission-button behavior change.

## Deliver

`npm test` passed 910 tests, including build and quality gates. `npm run quality:check`,
`git diff --check` and `graphify update .` passed. Manual Extension Development Host smoke remains pending: enter local
and remote case-only collisions, correct the name, use custom patterns, prepare an existing sync name,
and switch repositories while editing. Verify status announcements and successful submission afterward.
Rollback this preview enhancement and retain submission checks and user configuration. Remaining UX
item: modal keyboard focus handling.
