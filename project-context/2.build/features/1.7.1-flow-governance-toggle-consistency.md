# Flow Governance toggle consistency — 1.7.1

Status: implemented; automated verification passed; manual smoke pending
Date: 2026-09-05

## Define

First incremental slice of the maintainer-authorized Flow Governance UX improvements.
Acceptance: activation reflects persisted, validated configuration; saving is visible and prevents
repeat submissions; failures clear saving; completions cannot change another repository or a disposed
surface. The toggle continues to edit the repository-versioned configuration, now explained by its tooltip.

Further slices remain separate: actionable configuration diagnostics, retained branch-form input,
operation previews, early naming validation, and dialog keyboard navigation. This slice does not change
Git operations, branch policies, dependencies, or contribution IDs.

## Build

- `optionsWorkflow.ts` composed by `governanceWorkflow.ts`: retain confirmed activation while saving, reject duplicate pending requests,
  reread configuration after persistence, preserve current graph state, and guard repository/disposal boundaries.
- `flowTypes.ts`: optional host-owned saving state.
- `revisionGraphWebview.ts`, `bootstrap.ts`, `interactions.ts`, `viewOptionsToolbarUi.ts`: saving status,
  toggle availability and repository-file tooltip. Existing state messages carry the optional field;
  no new inbound message or permission is introduced.
- `orchestrationContracts.test.ts`, `revisionGraphWebview.test.ts`: deferred write outcomes, duplicate
  requests, invalid configuration, repository switch, disposal, and UI recovery.

## Deliver

Automated verification: `npm test` passed all 851 tests (includes build and the code-quality gate);
`git diff --check` passed. `graphify update .` completed after source changes.
The orchestration mock loader now reloads the composed options module to isolate each VS Code mock.
Manual Extension Development Host check remains pending: toggle a valid config, verify Saving… and
final badges; repeat with an unwritable file; switch repositories during persistence; check status
announcement with a screen reader. No packaging or publication performed.
Rollback: revert the scoped workflow/UI changes and optional saving field; preserve repository config files.
